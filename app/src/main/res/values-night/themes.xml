<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.USBSerialTelnetServer" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">true</item>
        <!-- Status bar on the top -->
        <item name="android:statusBarColor">@color/blue_even_darker</item>
        <item name="android:windowLightStatusBar" tools:targetApi="23">false</item>
        <!-- Title color -->
        <item name="titleColor">@color/blue_even_darker</item>
        <item name="titleTextColor">@color/white</item>
        <!-- Bottom panel color -->
        <item name="bottomPanelColor">@color/black</item>
        <item name="bottomPanelDividerColor">@color/white</item>
        <!-- Navigation bar color -->
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">false</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_even_darker</item>
        <item name="colorPrimaryVariant">@color/blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/text_color_dark</item>
        <item name="android:textColorPrimary">@color/text_color_dark</item>
        <item name="android:textColorHint">@color/text_color</item>
    </style>
</resources>